values = input('Give me some cs number: ')
list = values.split(',')
tuple = tuple(list)
print(list, tuple)

# cs = comma separated
# List are mutable
# Tuples are immutable
# data transfer we don't wanted messed with
# financial
